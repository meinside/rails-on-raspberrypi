!!!
%html
  %head
    %title
      = content_for?(:title) ? yield(:title) : 'Raspberry Pi server'
    %link{ rel: 'shortcut icon', href: '/favicon.ico'}/
    %meta{ 'http-equiv' => 'Content-Type', content: 'text/html; charset=utf-8' }/
    %meta{ name: 'viewport', content: 'user-scalable=yes, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0, width=device-width' }/
    %meta{ name: 'description', content: '' }/
    %meta{ name: 'author', content: '' }/
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = csrf_meta_tags
    = yield(:head)
  %body{ 'data-controller' => params[:controller], 'data-action' => params[:action] }
    / wrap all page content here
    %div#wrap
      / header here
      = render 'layouts/header'

      %div.container
        / message here
        = render 'layouts/messages'
        / main content here
        = yield

    / footer and ascii art here
    = render 'layouts/footer'
    = render 'layouts/ascii_art'

  / late javascript including for quick load
  %script
    (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
    (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
    m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
    })(window,document,'script','//www.google-analytics.com/analytics.js','ga');
    ga('create', 'UA-40130882-2', 'http01.com');
    ga('send', 'pageview');
